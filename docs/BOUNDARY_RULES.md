# Boundary Rules

- Constitution is READ-ONLY at runtime
- No layer may modify constitution values at runtime
- Amendments are a human-driven offline process
- All contracts are pure TypeScript interfaces (no implementations)
